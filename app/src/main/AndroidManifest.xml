<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.stressless">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Stressless">
        <activity
            android:exported="false"
            android:name=".ui.login.EnterMail"
            android:label="@string/app_name"
            android:theme="@style/Theme.Stressless.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.login.EnterPassword"/>
        <activity android:name=".ui.login.Register_User"/>
        <activity android:name=".MainActivity"/>
        <activity android:name=".ui.login.Register_Community"/>
        <activity android:name=".ui.login.Register_Or_Apply_To_Community"/>
    </application>
</manifest>